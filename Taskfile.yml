version: '3'

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task --list
    silent: true

  test:
    desc: Run all tests
    deps:
      - python:test
      - ts:test

  fmt:
    desc: Format all Python code
    cmds:
      - uv run --with ruff ruff format .

  lint:
    desc: Lint all Python code
    cmds:
      - uv run --with ruff ruff check .

  quality:
    desc: Run all quality checks (lint, typecheck, tests, validate)
    deps:
      - lint
      - ts:typecheck
      - ts:lint
      - python:coverage
      - ts:coverage
      - validate

  validate:
    desc: Validate all example documents
    cmds:
      - uv run --extra dev python validation/xbrief_validator.py examples/*.xbrief.json

  python:test:
    desc: Run libxbrief Python tests
    cmds:
      - uv run --extra dev pytest tests/ -v

  python:coverage:
    desc: Run libxbrief Python tests with coverage report
    cmds:
      - uv run --extra dev pytest tests/ --cov=libxbrief --cov-report=term-missing --cov-fail-under=85

  ts:install:
    desc: Install libxbrief-ts dependencies
    dir: libxbrief-ts
    cmds:
      - npm install

  ts:typecheck:
    desc: Run libxbrief-ts type checking
    dir: libxbrief-ts
    cmds:
      - npm run typecheck

  ts:lint:
    desc: Run libxbrief-ts lint checks
    dir: libxbrief-ts
    cmds:
      - npm run lint

  ts:test:
    desc: Run libxbrief-ts tests
    dir: libxbrief-ts
    cmds:
      - npm run test

  ts:coverage:
    desc: Run libxbrief-ts tests with coverage
    dir: libxbrief-ts
    cmds:
      - npm run test:coverage

  ts:build:
    desc: Build the libxbrief-ts package
    dir: libxbrief-ts
    cmds:
      - npm run build

  git:hooks:install:
    desc: Install local git hooks (pre-commit + commit-msg)
    cmds:
      - |
        set -euo pipefail
        mkdir -p .git/hooks
        cp -f tools/githooks/pre-commit .git/hooks/pre-commit
        cp -f tools/githooks/commit-msg .git/hooks/commit-msg
        chmod +x .git/hooks/pre-commit .git/hooks/commit-msg
        echo "Installed git hooks into .git/hooks"
